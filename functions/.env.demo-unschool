# Loaded only when the emulators run with --project demo-unschool.
# Local values, no secrets.

SCHOOL_EMAIL=school@example.test
SUPPORT_EMAIL=support@example.test
MAIL_FROM=Unschool local <noreply@example.test>

PAYMENT_PROVIDER=mock
ALLOW_DEMO_ORDERS=true

# Sign in through the Auth emulator with this address to reach /admin/.
ADMIN_EMAILS=admin@example.test

DOWNLOAD_TTL_MINUTES=4320

rules_version = '2';

// Every read and write goes through Cloud Functions using the Admin SDK.
// Browsers never touch Firestore directly, including the admin page.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
